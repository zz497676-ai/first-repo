# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository is currently empty of code — it contains only a `README.md`. There is no build system, package manager, source tree, test suite, or lint configuration yet.

When adding the first real code to this repository:
- Update this file with actual build/lint/test commands once a language and toolchain are chosen.
- Document the high-level architecture here as it emerges, rather than letting this file go stale.
